# Local Three.js vendor

- `three.module.js` — Three.js r160 (MIT License, see `LICENSE`)
- `OrbitControls.js` — official addon from the same release, imports `"three"` via the import map in `index.html`

These files are pinned locally so the project runs with zero build step and no CDN dependency.
